// Constante: los segundos registrados
const segundos = 7200;

// Variables: conversiones a minutos, horas y días
let minutos = segundos / 60;
let horas = segundos / 3600;
let dias = segundos / 86400;

// Mostrar resultados
alert("Los " + segundos + " segundos equivalen a:");
alert(minutos + " minutos");
alert(horas + " horas");
alert(dias + " días");

console.log("Los " + segundos + " segundos equivalen a:");
console.log(minutos + " minutos");
console.log(horas + " horas");
console.log(dias + " días");
